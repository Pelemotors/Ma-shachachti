"use client";import {useRef,useState} from "react";import {authHeaders} from "@/lib/supabase-browser";
export function SupportReport(){const[open,setOpen]=useState(false),[text,setText]=useState(""),[file,setFile]=useState<File|null>(null),[busy,setBusy]=useState(false),[msg,setMsg]=useState("");const input=useRef<HTMLInputElement>(null);async function send(){if(!text.trim())return setMsg("כתבי בקצרה מה קרה.");setBusy(true);setMsg("");const f=new FormData();f.set("description",text);f.set("page",location.href);f.set("userAgent",navigator.userAgent);if(file)f.set("media",file);const r=await fetch("/api/support/report",{method:"POST",headers:await authHeaders(),body:f});setBusy(false);if(!r.ok){const d=await r.json().catch(()=>null);return setMsg(d?.error??"לא הצלחנו לשלוח.")}setMsg("נשלח ✓ תודה, נטפל בזה.");setText("");setFile(null);setTimeout(()=>setOpen(false),1200)}return <>{<button aria-label="דיווח על תקלה" title="דיווח על תקלה" onClick={()=>setOpen(true)} style={{position:"fixed",left:16,bottom:20,zIndex:30,border:0,borderRadius:999,padding:"12px 16px",boxShadow:"0 4px 18px #0002"}}>⚠️ תקלה?</button>}{open&&<div role="dialog" aria-modal="true" style={{position:"fixed",inset:0,zIndex:40,background:"#0006",display:"grid",alignItems:"end"}} onClick={e=>{if(e.target===e.currentTarget)setOpen(false)}}><section className="panel stack" style={{margin:12,maxHeight:"90vh",overflow:"auto"}}><div className="row" style={{justifyContent:"space-between"}}><h2>משהו לא עובד?</h2><button className="text-button" onClick={()=>setOpen(false)}>סגירה</button></div><p>צלמי את התקלה או צרפי תמונה, וכתבי במשפט מה קרה. זה יגיע ישירות לצוות.</p><textarea rows={4} placeholder="לדוגמה: לחצתי על 'מה שכחתי?' ולא קרה כלום" value={text} onChange={e=>setText(e.target.value)}/><input ref={input} type="file" accept="image/*,video/mp4" capture="environment" hidden onChange={e=>setFile(e.target.files?.[0]??null)}/><button className="secondary" onClick={()=>input.current?.click()}>{file?`📎 ${file.name}`:"📷 צילום / צירוף מסך"}</button><button className="primary" disabled={busy} onClick={send}>{busy?"שולח…":"שליחת דיווח"}</button>{msg&&<p>{msg}</p>}<small>הדיווח כולל את העמוד וסוג המכשיר כדי שנוכל לאתר את התקלה. כתובת המייל של הצוות אינה מוצגת.</small></section></div>}</>}
