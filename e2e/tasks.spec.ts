import { test } from "@playwright/test";

/**
 * Covered by e2e/p70-defer.spec.ts (local seeded demo).
 * This file kept only so historical suite names stay discoverable.
 */
test.describe("tasks", () => {
  test.skip("legacy placeholder — see P70 in e2e/p70-defer.spec.ts", async () => {});
});
