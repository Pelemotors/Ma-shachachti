"use client";
import { useState } from "react";
import { supabase } from "@/lib/supabase-browser";
function returnTarget(){const raw=new URLSearchParams(location.search).get("returnTo")||"/app";return raw.startsWith("/app")&&!raw.startsWith("//")?raw:"/app"}
export default function LoginPage(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[signup,setSignup]=useState(false),[busy,setBusy]=useState(false),[message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();if(!supabase)return;setBusy(true);setMessage("");try{if(signup){const{data,error}=await supabase.auth.signUp({email,password});if(error)throw error;if(data.session)location.href=returnTarget();else setMessage("החשבון נוצר. לאחר אישור מנהל אפשר יהיה להיכנס.");}else{const{error}=await supabase.auth.signInWithPassword({email,password});if(error)throw error;location.href=returnTarget();}}catch{setMessage(signup?"לא הצלחנו ליצור חשבון. בדקי את הפרטים ונסי שוב.":"האימייל או הסיסמה אינם נכונים.");}finally{setBusy(false)}}async function reset(){if(!supabase||!email){setMessage("הזיני אימייל כדי לאפס סיסמה.");return}setBusy(true);try{const{error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/app`});if(error)throw error;setMessage("שלחנו קישור לאיפוס סיסמה לאימייל.");}catch{setMessage("לא הצלחנו לשלוח קישור לאיפוס סיסמה.");}finally{setBusy(false)}}return <main className="welcome"><div className="brand-mark">מ׳</div><p className="eyebrow">מה שכחתי?</p><h1>{signup?"יצירת חשבון":"כניסה לאזור האישי"}</h1><form className="panel stack" onSubmit={submit}><label>אימייל<input type="email" dir="ltr" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>סיסמה<input type="password" dir="ltr" autoComplete={signup?"new-password":"current-password"} minLength={6} required value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="primary" disabled={busy}>{busy?"רגע…":signup?"יצירת חשבון":"כניסה"}</button></form>{!signup&&<button className="text-button" disabled={busy} onClick={reset}>שכחתי סיסמה</button>}<button className="text-button" onClick={()=>{setSignup(!signup);setMessage("")}}>{signup?"כבר יש לי חשבון — כניסה":"אין לי חשבון — הרשמה"}</button>{message&&<p className={message.includes("נוצר")||message.includes("שלחנו")?"muted":"error"}>{message}</p>}</main>}
