import { authorize,ApiError,fail } from "@/lib/server";
const MAX=8*1024*1024;
const esc=(s:string)=>s.replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]!));
export async function POST(req:Request){try{const {userId}=await authorize(req);const key=process.env.RESEND_API_KEY,to=process.env.SUPPORT_REPORT_TO,from=process.env.SUPPORT_REPORT_FROM;if(!key||!to||!from)throw new ApiError(503,"שליחת דיווחים עדיין לא הופעלה.");const form=await req.formData();const description=String(form.get("description")??"").trim().slice(0,4000);const page=String(form.get("page")??"").slice(0,1000);const ua=String(form.get("userAgent")??"").slice(0,1000);const file=form.get("media");if(!description)throw new ApiError(400,"צריך לכתוב בקצרה מה קרה.");const attachments:any[]=[];if(file instanceof File&&file.size){if(file.size>MAX)throw new ApiError(413,"הקובץ גדול מדי. אפשר עד 8MB.");if(!["image/png","image/jpeg","image/webp","video/mp4"].includes(file.type))throw new ApiError(415,"סוג הקובץ אינו נתמך.");attachments.push({filename:file.name||"report-media",content:Buffer.from(await file.arrayBuffer()).toString("base64"),content_type:file.type});}
const html=`<div dir="rtl" style="font-family:Arial"><h2>דיווח תקלה חדש — מה שכחתי?</h2><p><b>מה קרה:</b><br>${esc(description).replace(/\n/g,"<br>")}</p><hr><p><b>משתמש:</b> ${esc(userId)}</p><p><b>עמוד:</b> ${esc(page)}</p><p><b>מכשיר:</b> ${esc(ua)}</p><p><b>זמן:</b> ${new Date().toISOString()}</p></div>`;const r=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify({from,to:[to],subject:"דיווח תקלה חדש — מה שכחתי?",html,attachments})});if(!r.ok)throw new ApiError(502,"הדיווח לא נשלח. נסי שוב.");return Response.json({ok:true});}catch(e){return fail(e)}}